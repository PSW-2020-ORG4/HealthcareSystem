version: "3.8"
services: 
  database:
    image: postgres:13-alpine
    restart: always
    environment:
      POSTGRES_USER: "org4"
      POSTGRES_PASSWORD: "org4"
      POSTGRES_DB: "organization4db"
    networks:
      - demo